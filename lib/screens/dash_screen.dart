import 'package:flutter/material.dart';

/// Dash Screen the home page of the connected User
class DashScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        //TODO: Design the Dash Screen.

        );
  }
}
